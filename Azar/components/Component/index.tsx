import styles from "./index.module.scss";
const Component = () => {
    return (
        <div className={styles.poc}>
            <label className={styles.poc_text}>Test template project </label>
        </div>
    );
};

export default Component;
