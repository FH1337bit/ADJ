import styles from "../Company/index.module.scss";

const Company = () => {
    return (
        <div className={styles.company}>
            <h3>JOIN US NOW</h3>
            <h1>Any obstacle is an opportunity in disguise.</h1>
        </div>
    );
};

export default Company;
